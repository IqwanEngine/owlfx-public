// Generated by IqwanEngine
import React, { useEffect, useState } from 'react';
import {
  ShieldCheck,
  TrendingUp,
  Cpu,
  ArrowUpRight,
  Award,
  Zap,
  Users,
  MapPin,
  BarChart3,
  Radio,
  Sparkles
} from 'lucide-react';
import { useLanguage } from '../context/LanguageContext';

interface HeroSectionProps {
  onExploreAlgo: () => void;
}

export const HeroSection: React.FC<HeroSectionProps> = ({
  onExploreAlgo,
}) => {
  const { t } = useLanguage();
  const [traderCount, setTraderCount] = useState(5840);

  useEffect(() => {
    const interval = setInterval(() => {
      setTraderCount(prev => prev + Math.floor(Math.random() * 3) + 1);
    }, 4500);
    return () => clearInterval(interval);
  }, []);

  return (
    <section
      id="hero-section"
      className="relative min-h-[94vh] pt-28 sm:pt-36 pb-20 flex flex-col justify-center overflow-hidden selection:bg-[#D4AF37] selection:text-black"
    >
      {/* ======================================================================= */}
      {/* CRISP INNER-GLASS SHINE ENGINE (NO OUTER GLOW / NO BLUR SMEAR)          */}
      {/* ======================================================================= */}
      <style>{`
        @keyframes glassShimmer {
          0% {
            background-position: -200% 0;
          }
          100% {
            background-position: 200% 0;
          }
        }
        .silver-glass-clean {
          background: linear-gradient(
            115deg,
            #94a3b8 0%,
            #e2e8f0 20%,
            #ffffff 45%,
            #ffffff 55%,
            #cbd5e1 75%,
            #94a3b8 100%
          );
          background-size: 200% auto;
          -webkit-background-clip: text;
          -webkit-text-fill-color: transparent;
          animation: glassShimmer 6s linear infinite;
        }
        .gold-glass-clean {
          background: linear-gradient(
            115deg,
            #b45309 0%,
            #f59e0b 20%,
            #fef08a 42%,
            #ffffff 50%,
            #fef08a 58%,
            #d97706 78%,
            #b45309 100%
          );
          background-size: 200% auto;
          -webkit-background-clip: text;
          -webkit-text-fill-color: transparent;
          animation: glassShimmer 5s linear infinite;
        }
      `}</style>

      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 w-full relative z-20">

        {/* ======================================================================= */}
        {/* 1. INSTITUTIONAL STATUS PILL WITH DYNAMIC LASER ORBIT                   */}
        {/* ======================================================================= */}
        <div className="flex items-center justify-center mb-8 select-none">
          <div className="relative group p-[1.5px] rounded-full overflow-hidden transition-all duration-500 hover:scale-[1.03] hover:shadow-[0_0_35px_rgba(16,185,129,0.25)]">

            {/* Ambient Laser Spinning Border */}
            <span className="absolute inset-[-100%] bg-[conic-gradient(from_90deg_at_50%_50%,transparent_0%,#10B981_25%,transparent_50%,#F59E0B_75%,transparent_100%)] animate-[spin_5s_linear_infinite] opacity-70 group-hover:opacity-100 transition-opacity duration-500" />

            {/* Soft Glow Backdrop */}
            <div className="absolute inset-0 rounded-full bg-gradient-to-r from-emerald-500/10 to-amber-500/10 blur-sm pointer-events-none" />

            {/* Core Pill Container */}
            <div className="relative flex items-center gap-2.5 sm:gap-3 px-4 sm:px-5 py-2 rounded-full bg-slate-950/85 backdrop-blur-md md:backdrop-blur-2xl border border-white/[0.08] shadow-inner transition-colors duration-300 group-hover:bg-slate-950/75">

              {/* Radar Dot (Emerald Sync) */}
              <span className="relative flex h-2 w-2 shrink-0">
                <span className="animate-ping absolute inline-flex h-full w-full rounded-full bg-emerald-400 opacity-75" />
                <span className="relative inline-flex rounded-full h-2 w-2 bg-emerald-400 shadow-[0_0_10px_#34d399]" />
              </span>

              {/* Status Live */}
              <div className="flex items-center gap-1.5 font-mono text-[11px] sm:text-xs font-semibold text-emerald-300 tracking-wider uppercase">
                <Radio className="w-3.5 h-3.5 text-emerald-400 animate-pulse shrink-0" />
                <span className="drop-shadow-[0_0_8px_rgba(52,211,153,0.3)]">
                  {t.hero.statusLive}
                </span>
              </div>

              {/* Divider */}
              <span className="h-3 w-[1px] bg-gradient-to-b from-transparent via-slate-700 to-transparent shrink-0" />

              {/* Ecosystem Badge */}
              <div className="flex items-center gap-1.5 font-mono text-[11px] sm:text-xs font-bold tracking-widest uppercase">
                <Sparkles className="w-3.5 h-3.5 text-amber-400 shrink-0 transition-transform duration-300 group-hover:rotate-12" />
                <span className="bg-gradient-to-r from-amber-200 via-amber-400 to-yellow-500 bg-clip-text text-transparent drop-shadow-[0_0_10px_rgba(245,158,11,0.2)]">
                  {t.hero.ecosystem}
                </span>
              </div>

            </div>
          </div>
        </div>

        {/* ======================================================================= */}
        {/* 2. TAJAM & BERSIH: HEADLINE KACA TANPA GLOW KELUAR                     */}
        {/* ======================================================================= */}
        <div className="text-center max-w-5xl mx-auto select-none">
          <h1 className="text-4xl sm:text-6xl md:text-7xl lg:text-8xl font-black tracking-tight leading-[1.08] uppercase">
            {/* Headline 1: White-Silver Liquid Crystal Glass (Bersih & Tajam) */}
            <span className="block silver-glass-clean">
              {t.hero.headline1}
            </span>

            {/* Headline 2 & 3: Imperial Gold & Crystal White Clean Shine */}
            <span className="relative inline-block mt-2 sm:mt-3">
              <span className="relative gold-glass-clean">
                {t.hero.headline2} {t.hero.headline3}
              </span>
            </span>
          </h1>

          <p className="mt-8 sm:mt-10 text-base sm:text-xl text-slate-400 font-medium leading-relaxed max-w-3xl mx-auto tracking-wide">
            {t.hero.description}
          </p>

          {/* ======================================================================= */}
          {/* 3. HIGH-VOLTAGE HAPTIC BUTTON CLUSTER (OVERPOWER CTA)                   */}
          {/* ======================================================================= */}
          <div className="mt-12 flex flex-col sm:flex-row items-center justify-center gap-5 sm:gap-7">

            {/* Button 1: Malaysia Community (Hyper-Charged Gold Reactor) */}
            <a
              id="hero-cta-malaysia"
              href="/interested?action=register"
              target="_blank"
              rel="noopener noreferrer"
              className="relative group p-[1.5px] rounded-2xl overflow-hidden transition-all duration-500 hover:scale-[1.04] active:scale-[0.97] cursor-pointer shadow-[0_0_30px_rgba(245,158,11,0.35)] hover:shadow-[0_0_50px_rgba(245,158,11,0.6)]"
            >
              {/* Animated Conic Border Sweep */}
              <span className="absolute inset-0 bg-[conic-gradient(from_0deg_at_50%_50%,#78350f_0%,#f59e0b_50%,#fbbf24_100%)] animate-[spin_3s_linear_infinite]" />

              <div className="relative px-8 py-4 rounded-2xl bg-gradient-to-r from-amber-500 via-amber-400 to-amber-600 flex items-center justify-center space-x-3 overflow-hidden">
                {/* Glass Glare Dynamic Shimmer */}
                <div className="absolute inset-0 -translate-x-full group-hover:translate-x-full duration-1000 bg-gradient-to-r from-transparent via-white/40 to-transparent transform skew-x-12 transition-transform ease-out" />

                <span className="text-xl group-hover:scale-125 group-hover:rotate-6 transition-transform duration-300 drop-shadow-md">🇲🇾</span>
                <span className="font-mono font-black text-sm tracking-widest uppercase text-slate-950">
                  {t.hero.btnMalaysia}
                </span>
                <div className="p-1 rounded-lg bg-black/10 group-hover:bg-black/20 transition-colors">
                  <ArrowUpRight className="w-4 h-4 text-slate-950 group-hover:translate-x-0.5 group-hover:-translate-y-0.5 transition-transform duration-300" />
                </div>
              </div>
            </a>

            {/* Button 2: Indonesia Registration (Crimson Laser Surge) */}
            <a
              id="hero-cta-indonesia"
              href="/id/register"
              target="_blank"
              rel="noopener noreferrer"
              className="relative group p-[1.5px] rounded-2xl overflow-hidden transition-all duration-500 hover:scale-[1.04] active:scale-[0.97] cursor-pointer shadow-[0_0_30px_rgba(225,29,72,0.3)] hover:shadow-[0_0_50px_rgba(225,29,72,0.6)]"
            >
              {/* Animated Crimson Conic Border Sweep */}
              <span className="absolute inset-0 bg-[conic-gradient(from_180deg_at_50%_50%,#4c0519_0%,#f43f5e_50%,#fda4af_100%)] animate-[spin_3s_linear_infinite]" />

              <div className="relative px-8 py-4 rounded-2xl bg-gradient-to-r from-rose-700 via-red-600 to-amber-700 flex items-center justify-center space-x-3 overflow-hidden">
                {/* Specular White Shimmer Overlay */}
                <div className="absolute inset-0 -translate-x-full group-hover:translate-x-full duration-1000 bg-gradient-to-r from-transparent via-white/30 to-transparent transform skew-x-12 transition-transform ease-out" />

                <span className="text-xl group-hover:scale-125 group-hover:-rotate-6 transition-transform duration-300 drop-shadow-md">🇮🇩</span>
                <span className="font-mono font-black text-sm tracking-widest uppercase text-white drop-shadow-sm">
                  {t.hero.btnIndonesia}
                </span>
                <div className="p-1 rounded-lg bg-white/10 group-hover:bg-white/20 transition-colors">
                  <ArrowUpRight className="w-4 h-4 text-white group-hover:translate-x-0.5 group-hover:-translate-y-0.5 transition-transform duration-300" />
                </div>
              </div>
            </a>

            {/* Button 3: Quick Algo Demo (Quantum HUD Cyber Matrix) */}
            <button
              id="hero-cta-algo-preview"
              type="button"
              onClick={onExploreAlgo}
              className="relative group w-fit inline-flex items-center justify-center px-5 py-3 rounded-2xl font-mono text-xs font-bold text-cyan-300 bg-slate-950/90 border border-cyan-500/50 shadow-[0_0_20px_rgba(6,182,212,0.25)] cursor-pointer transition-all duration-300 hover:border-cyan-400 hover:shadow-[0_0_35px_rgba(6,182,212,0.5)] hover:scale-[1.03] active:scale-[0.97] backdrop-blur-md md:backdrop-blur-xl overflow-hidden shrink-0"
            >
              {/* Cyber Grid Pattern */}
              <div className="absolute inset-0 bg-repeat bg-center bg-[radial-gradient(#06b6d4_1.2px,transparent_1.2px)] [background-size:10px_10px] opacity-20 group-hover:opacity-40 transition-opacity pointer-events-none" />

              {/* Hover Glow Background */}
              <div className="absolute inset-0 bg-cyan-500/5 group-hover:bg-cyan-500/15 transition-colors pointer-events-none" />

              {/* Content */}
              <div className="relative z-10 flex items-center space-x-2.5 whitespace-nowrap">
                <div className="p-1 rounded-lg bg-cyan-500/20 border border-cyan-500/40 group-hover:border-cyan-400 transition-colors shrink-0">
                  <Cpu className="w-4 h-4 text-cyan-400 group-hover:rotate-180 transition-transform duration-700 ease-in-out" />
                </div>
                <span className="tracking-wider uppercase font-extrabold whitespace-nowrap">
                  {t.hero.btnAlgoPreview}
                </span>
              </div>
            </button>
          </div>

          {/* ======================================================================= */}
          {/* 4. INSTITUTIONAL AUDIT BADGES WITH METRIC PINS                          */}
          {/* ======================================================================= */}
          <div className="mt-10 flex flex-wrap items-center justify-center gap-6 sm:gap-8 text-xs text-slate-400 font-mono">
            <div className="flex items-center space-x-2 px-3 py-1.5 rounded-lg bg-white/[0.02] border border-white/5">
              <ShieldCheck className="w-4 h-4 text-[#D4AF37]" />
              <span className="tracking-wide">{t.hero.badgeLiquidity}</span>
            </div>
            <span className="text-slate-800 hidden sm:inline">•</span>
            <div className="flex items-center space-x-2 px-3 py-1.5 rounded-lg bg-white/[0.02] border border-white/5">
              <Zap className="w-4 h-4 text-cyan-400" />
              <span className="tracking-wide">{t.hero.badgeExecution}</span>
            </div>
            <span className="text-slate-800 hidden sm:inline">•</span>
            <div className="flex items-center space-x-2 px-3 py-1.5 rounded-lg bg-white/[0.02] border border-white/5">
              <Award className="w-4 h-4 text-emerald-400" />
              <span className="tracking-wide">{t.hero.badgeLicensed}</span>
            </div>
          </div>
        </div>

        {/* ======================================================================= */}
        {/* 5. 3D QUANTUM METRICS TICKER HUD (INSTITUTIONAL GRADE)                  */}
        {/* ======================================================================= */}
        <div
          id="hero-metrics-ticker-bar"
          className="mt-16 w-full rounded-3xl bg-slate-950/70 backdrop-blur-md md:backdrop-blur-2xl border border-white/10 p-6 sm:p-10 shadow-[0_20px_50px_rgba(0,0,0,0.8),0_0_40px_rgba(212,175,55,0.08)] relative overflow-hidden group"
        >
          {/* Top Golden Laser Line with Traveling Light Spot */}
          <div className="absolute top-0 left-0 right-0 h-[1px] bg-gradient-to-r from-transparent via-[#D4AF37]/80 to-transparent" />
          <div className="absolute top-0 -left-1/4 w-1/2 h-[2px] bg-gradient-to-r from-transparent via-amber-300 to-transparent animate-[pulse_3s_ease-in-out_infinite]" />

          {/* Background Matrix Pattern Accent */}
          <div className="absolute inset-0 bg-[radial-gradient(rgba(212,175,55,0.08)_1px,transparent_1px)] [background-size:24px_24px] pointer-events-none opacity-40" />

          <div className="relative grid grid-cols-2 lg:grid-cols-4 gap-8 md:gap-10 text-center lg:divide-x divide-white/10">
            {/* Metric 1: Verified Traders */}
            <div className="flex flex-col items-center justify-center p-3 rounded-2xl hover:bg-white/[0.02] transition-colors">
              <div className="flex items-center space-x-2 mb-3">
                <div className="p-2 rounded-xl bg-cyan-500/10 border border-cyan-500/30 text-cyan-400 shadow-[0_0_15px_rgba(6,182,212,0.2)]">
                  <Users className="w-4 h-4" />
                </div>
                <span className="text-[10px] font-mono font-bold uppercase tracking-[0.2em] text-cyan-300/90">{t.hero.statTraders}</span>
              </div>
              <div className="text-3xl sm:text-4xl font-mono font-extrabold text-white tracking-tight flex items-baseline gap-1.5">
                <span>{traderCount.toLocaleString()}</span>
                <span className="text-xs font-mono font-bold text-emerald-400 px-1.5 py-0.5 rounded bg-emerald-500/10 border border-emerald-500/20">
                  MY & ID
                </span>
              </div>
              <p className="text-[11px] text-slate-500 mt-2 font-mono uppercase tracking-widest">{t.hero.statTradersSub}</p>
            </div>

            {/* Metric 2: National Footprint */}
            <div className="flex flex-col items-center justify-center p-3 rounded-2xl hover:bg-white/[0.02] transition-colors lg:pl-8">
              <div className="flex items-center space-x-2 mb-3">
                <div className="p-2 rounded-xl bg-amber-500/10 border border-amber-500/30 text-amber-400 shadow-[0_0_15px_rgba(245,158,11,0.2)]">
                  <MapPin className="w-4 h-4" />
                </div>
                <span className="text-[10px] font-mono font-bold uppercase tracking-[0.2em] text-amber-400/90">14 NEGERI</span>
              </div>
              <div className="text-3xl sm:text-4xl font-mono font-extrabold text-white tracking-tight">
                14 <span className="text-slate-500 text-lg font-medium">Negeri</span>
              </div>
              <p className="text-[11px] text-slate-500 mt-2 font-mono uppercase tracking-widest">Ekspedisi Jelajah Kebangsaan</p>
            </div>

            {/* Metric 3: Validated Winrate */}
            <div className="flex flex-col items-center justify-center p-3 rounded-2xl hover:bg-white/[0.02] transition-colors lg:pl-8">
              <div className="flex items-center space-x-2 mb-3">
                <div className="p-2 rounded-xl bg-emerald-500/10 border border-emerald-500/30 text-emerald-400 shadow-[0_0_15px_rgba(16,185,129,0.25)]">
                  <BarChart3 className="w-4 h-4" />
                </div>
                <span className="text-[10px] font-mono font-bold uppercase tracking-[0.2em] text-emerald-400/90">{t.hero.statWinrate}</span>
              </div>
              <div className="text-3xl sm:text-4xl font-mono font-extrabold text-emerald-400 drop-shadow-[0_0_15px_rgba(52,211,153,0.4)] tracking-tight">
                84.2<span className="text-emerald-300 text-xl font-medium">%</span>
              </div>
              <p className="text-[11px] text-slate-500 mt-2 font-mono uppercase tracking-widest">{t.hero.statWinrateSub}</p>
            </div>

            {/* Metric 4: Total Ecosystem Volume */}
            <div className="flex flex-col items-center justify-center p-3 rounded-2xl hover:bg-white/[0.02] transition-colors lg:pl-8">
              <div className="flex items-center space-x-2 mb-3">
                <div className="p-2 rounded-xl bg-amber-500/10 border border-amber-500/30 text-amber-400 shadow-[0_0_15px_rgba(212,175,55,0.3)]">
                  <TrendingUp className="w-4 h-4" />
                </div>
                <span className="text-[10px] font-mono font-bold uppercase tracking-[0.2em] text-amber-300/90">{t.hero.statVolume}</span>
              </div>
              <div className="text-3xl sm:text-4xl font-mono font-black text-transparent bg-clip-text bg-gradient-to-r from-amber-200 via-amber-400 to-[#D4AF37] drop-shadow-[0_0_20px_rgba(212,175,55,0.4)] tracking-tight">
                $18.4M<span className="text-amber-400 font-bold">+</span>
              </div>
              <p className="text-[11px] text-slate-500 mt-2 font-mono uppercase tracking-widest">{t.hero.statVolumeSub}</p>
            </div>
          </div>
        </div>

      </div>
    </section>
  );
};
